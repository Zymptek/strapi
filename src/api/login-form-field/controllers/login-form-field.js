'use strict';

/**
 * login-form-field controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::login-form-field.login-form-field');
